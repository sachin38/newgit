/* program to find length of a given string */

#include <stdio.h>
#include <string.h> 

int main()
{
   char str[]="Hello World!";
   printf("%s",str);
   printf("\nThe size of string is: %d\n",sizeof(str));

   return(0);
}
